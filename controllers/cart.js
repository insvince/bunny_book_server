import { Book, User, Cart } from '../models/model';
import jwt from 'jsonwebtoken';

const cartController = {
    addCart: async (req, res) => {
        // Check login to add cart
        // Add to cart
        // Check product exist in cart
        // Push it on the database
    },
};
